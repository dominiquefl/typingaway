
<%= form_with(model: [ @article, @article.comments.build ], local: true) do |form| %>
  <p>
    <%= form.label 'Name' %><br>
    <%= form.text_field :commenter %>
  </p>

  <p>
    <%= form.label 'Thought' %><br>
    <%= form.text_area :body %>
  </p>

  <p>
    <%= form.submit class: 'btn btn-default btn-large button button-form', value: 'Comment' %>
  </p>
<% end %>
